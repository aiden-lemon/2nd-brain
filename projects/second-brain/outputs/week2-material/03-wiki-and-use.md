# 위키화·업무 활용 21분

> 2주차 교육 자료 · 2026-08-28 · 활용부

변환된 마크다운이 위키 노트가 되고, 그 위키를 업무에 쓰는 뒷단을 다룬다. 앞단(변환)은
변환부에서 끝났다. 전 과정은 화면 공유로 시연한다.

---

## 08 2단계: 위키로 쌓기

> 7분 · 슬라이드 2장

이 섹션을 보면 변환이 끝난 파일이 어떤 판단을 거쳐 위키 노트가 되는지 안다. 인제스트가
모든 파일을 노트로 만들지 않는다는 것이 요점이다.

### 변환과 위키화의 분리

변환 스킬은 `Clippings/`에 마크다운을 놓는 데까지만 한다. 위키화는 `vault-ingest`가
이어받는다. 한 스킬이 둘 다 하지 않는 이유는 판단의 성격이 다르기 때문이다. 변환은
포맷을 바꾸는 일이고, 위키화는 무엇을 남길지 고르는 일이다.

파일은 세 자리를 지난다.

- `Clippings/` — 변환된 마크다운이 들어온다. 위키화를 기다리는 자리다. 처리되면 파일이 `raw/`로 옮겨져 비워진다
- `raw/` — 원문을 그대로 보관한다. 이후 수정하지 않는다
- `wiki/` + `wiki/INDEX.md` — 다시 꺼내 쓸 개념만 노트로 쓰고 색인에 한 줄 올린다

인제스트 트리거는 채팅에 `인제스트해줘` 한 줄이다.

노트에는 출처가 `sources: ["raw/<파일명>.md"]` 문자열로 박힌다. 나중에 "이건 어디서 나온
얘기지"를 원문까지 되짚을 수 있다.

원본을 그냥 검색에 던지는 방식과 대비된다. 원본을 조각내 검색하면 조각이 문맥을 잃는다.
Anthropic이 든 예시는 `The company's revenue grew by 3% over the previous quarter.` 한
줄만 남아 어느 회사, 어느 분기인지 알 수 없게 되는 경우다. 정제 노트는 제목·출처·요약을
갖고 있어 잘려도 문맥이 남는다. 그리고 노트가 늘어나도 `wiki/INDEX.md` 색인부터 읽으므로
긴 문서 중간에 정보가 묻히지 않는다.

**화면**: `Clippings/` → `raw/` → `wiki/` + `wiki/INDEX.md` 세 칸 그림. 변환 스킬이 맡는
구간과 `vault-ingest`가 맡는 구간을 선으로 나눠 표시한다
**말할 것**: 개발자 — 변환 산출물과 지식 계층을 같은 커밋에 섞지 않는 구조다. 기획자 —
"복사해서 옮기는 게 아니라, 옮길 것을 먼저 고른다"
**근거**: `week2-sources/05-work-usage.md` § 1 (스킬 3종은 변환까지만 한다) ·
`week2-sources/04-knowledge-base.md` § 5 (Anthropic Contextual Retrieval 2024-09-19,
Lost in the Middle arXiv 2307.03172)

---

### 인제스트의 보류 판단

인제스트는 옮기는 작업이 아니라 판단하는 작업이다. 들어온 것을 다 위키로 만들지 않는다.

2026-08-28 실행에서 `Clippings/` 2건을 처리했다. Reddit 게시글 1건에서는 wiki 노트 4건이
나왔고, `점검-결과-보고.md`(hwpx 변환 산출물, 403자)는 wiki 노트를 만들지 않았다. 실행
로그의 사유는 이렇다.

> wiki 노트를 만들지 않았다. 실측이 아닌 합성 수치를 wiki article로 올리면 이 vault의
> 지식 계층에 검증 불가능한 도메인 사실이 들어간다. 원문은 `raw/점검-결과-보고.md`로,
> 바이너리 원본은 `raw/hwp/점검-결과-보고.hwpx`로 보존해 변환 파이프라인의 증거로만
> 남긴다.

판정 기준은 하나다 — 다시 꺼내 쓸 개념인가. 그렇다면 노트로, 아니면 원본으로만 남긴다.

보류 사유는 같은 로그의 `Dropped / Issues`에 남는다.

> `점검-결과-보고.md` — wiki화 보류. 사유는 위 "처리 2". 실제 설비 점검 데이터가 들어오면
> 그때 도메인 노트를 세우는 편이 낫다.

사유를 적어 두기 때문에 나중에 "이 문서는 넣었는데 왜 위키에 없지"를 로그에서 찾을 수
있다.

**화면**: `outputs/runs/2026-08-28-ingest-aiden-lemon.md`를 열어 프론트매터
(`processed: 2` · `new_notes: 4`)와 `처리 2` 문단, `Dropped / Issues` 첫 항목을 차례로
보여준다
**말할 것**: 설명이 아니라 실제 판단 기록이다. `processed: 2`인데 `new_notes: 4`이고, 그
4건은 전부 다른 파일에서 나왔다 — 나머지 1건은 노트가 0건이다
**근거**: `outputs/runs/2026-08-28-ingest-aiden-lemon.md` (인용 2건 모두 원문 그대로)

---

## 09 3단계: 업무에 쓰기

> 9분 · 슬라이드 2장 (`결과 확인` 슬라이드 시간을 포함해 9분)

이 섹션을 보면 쌓인 위키를 실제 업무에서 어떻게 꺼내 쓰는지 안다. 질의와 주간 보고서
두 가지를 시연한다.

### 위키 질의

지금 이 vault의 위키 상태다.

| 항목 | 값 |
| --- | --- |
| `wiki/` 아티클 / 토픽 | 4개 / 2개 |
| `wiki/INDEX.md` | 915 B |
| `Clippings/` | 0건 |
| `raw/` | 1건 |

아티클 4개는 1주차에 클리핑 1건을 넣은 결과다.

| 노트 | 무엇 |
| --- | --- |
| `claude-code` | 구독 인증으로 도는 터미널 코딩 에이전트 CLI |
| `claude-subscription-billing-boundary` | 구독·API 과금 경계와 누수 지점 |
| `hermes-agent` | 항상 켜져 있는 오케스트레이터 에이전트 런타임 |
| `multi-agent-role-separation` | 역할별로 도구를 나눠 맡기는 구성 패턴 |

수강자가 지난주에 한 일의 결과다. 남의 사례가 아니다.

`claude-subscription-billing-boundary.md`에는 `status: needs-update`가 붙어 있다. 근거가
Reddit 게시글과 댓글뿐이고 1차 출처로 확인되지 않아서다. 위키는 완성품이 아니라 갱신
대상이다.

질의는 `vault에서 ~ 정리해줘`로 시킨다. 다만 노트 4개로 답할 수 있는 범위는 좁다.
`vault-query`가 노트 몇 개부터 쓸모가 생기는지에 대한 실측 근거는 없다. 넣는 만큼 답이
좋아진다. 그게 과제의 이유다.

**화면**: `wiki/INDEX.md` 원문. 915 B라 한 화면에 다 들어간다. 이어서
`claude-subscription-billing-boundary.md` 상단의 `status: needs-update`와 본문 경고 문단
**말할 것**: 개발자 — 클리핑 1건이 개념 4개로 분해됐고 각 노트에 `status`가 따로 붙는다.
기획자 — "위키는 한 번 쓰고 끝나는 문서가 아니다. 사실이 바뀌면 갱신 표시가 붙는다"
**근거**: `week2-outline.md` § 실습 시작 상태 (2026-08-28 실측) · `wiki/INDEX.md` ·
`wiki/claude-subscription-billing-boundary.md` · `week2-sources/05-work-usage.md` § 5
(노트 개수 기준의 실측 근거는 없음)

---

### 주간 보고서

트리거는 `주간 보고`다. 산출은 `areas/weekly/YYYY-MM-DD.md`와 같은 이름의 `.html` 뷰 두
장이다. 입력이 git 변경 이력이라 사람이 따로 적을 게 없다.

2026-08-28 실제 산출값이다.

| 항목 | 값 |
| --- | --- |
| 커밋 (전체 / 실작업) | 20 / 14 |
| 머지된 PR | 5 |
| 변경 파일 | 40 |
| 삽입 / 삭제 | +3,876 / -115 |
| 신규 스킬 진입점 | 8 |
| 신규 wiki 문서 | 0 |

워크스트림은 4개로 묶였다 — 문서 변환 스킬 3종 반입, 온보딩 스크립트 실기기 결함 수정,
상위 vault 동기화 2회, README 반영. 각 워크스트림에 PR 번호와 날짜, 작업자가 붙는다.

숫자는 전수 집계다. 눈에 띄는 것만 고른 게 아니라 구간의 모든 커밋을 센다. 보고서
`Notes`에 집계 명령과 경계 커밋을 함께 적어 두기 때문에 나중에 같은 값을 다시 뽑을 수
있다.

```
git diff --shortstat 584c45f..master
git log --no-merges 584c45f..master
```

신규 wiki 문서 0이라는 값이 특히 유용하다. 도구는 갖췄지만 지식 축적은 아직 시작되지
않았다는 뜻이고, 그게 지금 수강자의 위치이기도 하다.

**화면**: `areas/weekly/2026-08-28.md`의 `Metrics` 표 → `Workstreams` 4개 제목 →
`Notes`의 집계 명령. 마지막에 같은 이름의 `.html` 뷰를 브라우저로 열어 나란히 보여준다
**말할 것**: 개발자 — 경계 커밋 `584c45f`를 `git rev-list -1 --before=` 로 잡았고 다음
보고서부터는 이 보고서를 만든 커밋이 경계가 된다. 기획자 — "지난 주에 뭐 했지를 기억으로
복원하지 않는다. 이력에서 뽑는다"
**근거**: `areas/weekly/2026-08-28.md` (Metrics · Workstreams · Notes) ·
`week2-sources/05-work-usage.md` § 1, § 5

---

## 결과 확인

> 시간은 09에 포함 · 슬라이드 1장

오늘 시연한 파일 한 건이 지금 어디에 어떤 상태로 남았는지 여섯 자리를 확인한다.

### 파일 하나가 지나는 자리

오늘 시연한 파일 한 건이 지금 어디에 어떤 상태로 있는지 확인한다.

| 위치 | 무엇이 | 이후 |
| --- | --- | --- |
| `Clippings/` | 변환된 마크다운 | 인제스트가 원문을 `raw/`로 **옮기면서** 비워진다 |
| `raw/hwp/` | 원본 `.hwpx` 그대로 | 수정·삭제하지 않는다 |
| `raw/` | 변환된 마크다운 원문 | 수정·삭제하지 않는다 |
| `wiki/` | 재사용할 개념만 노트로 | 사실이 바뀌면 갱신한다 |
| `wiki/INDEX.md` | 노트 목록 한 줄 | 질문할 때 여기부터 읽는다 |
| `outputs/runs/` | 실행 로그 — 판단과 보류 사유 | 왜 그랬는지의 근거 |

한 문장으로 정리하면 이렇다. 원본은 `raw/`에 그대로, 쓸 만한 개념은 `wiki/`에 정리해서,
왜 그렇게 판단했는지는 로그에 남는다. 세 가지가 분리돼 있어서 잘못 정리해도 되돌릴 수
있다.

**화면**: 여섯 위치를 실제로 `ls`로 훑고, `Clippings/`가 0건인 것과 `raw/hwp/`에
`.hwpx`가 그대로 있는 것을 확인한다
**말할 것**: 개발자 — `raw/`와 `archive/`는 append-only 계약이고 `vault_verify.py`가
검사한다. 기획자 — "원본은 절대 안 건드린다. 잘못 정리해도 되돌릴 수 있다"
**근거**: `week2-sources/05-work-usage.md` § 3 분기표 (`raw/` append-only 행)
· `week2-outline.md` § 실습 시작 상태

---

## 10 과제

> 3분 · 슬라이드 1장

수강자는 이 자리에서 실습하지 않는다. 대신 자기 문서 1건으로 같은 경로를 직접 돌린다.
다음 주 시작할 때 확인한다.

### 자기 문서 1건

자기 업무 문서 1건을 변환부터 인제스트까지 통과시킨다.

1. **문서를 고른다.** `.hwpx`가 가장 쉽다. 추가 설치가 없다. 회사 밖으로 나가면 안 되는
   문서는 고르지 않는다
2. **변환하고 검수한다.** 게이트 3개를 통과시킨다. 첫 줄과 표를 눈으로 본다. 머리말이
   본문에 섞였는지 확인한다
3. **인제스트한다.** 위키 노트가 생기는지, 아니면 보류되는지 본다. 보류됐으면 로그에 적힌
   사유를 읽는다
4. **물어본다.** 새로 넣은 내용을 vault에 질문한다. 답이 노트를 근거로 나오는지 확인한다

막히는 경우도 결과다. 게이트 1에서 "아니오"가 맞는 문서였다면 그걸 알아챈 것도 성과다. 그
문서는 vault 밖 변환 모드로 변환만 하고 결과를 vault에 넣지 않는다.

다음 주 시작할 때 확인한다. 한 건이면 된다.

**화면**: 4단계를 한 화면에 두고, 각 단계에 채팅에 적을 문구를 붙여 둔다 —
`이 한글 파일 마크다운으로 변환해서 Clippings에 넣어줘` · `인제스트해줘` ·
`vault에서 ~ 정리해줘`
**말할 것**: "우리 회사 문서를 넣어도 되나"는 여기서 답한다. 상용 조건(Team·Enterprise·
API)에서는 입력과 출력을 모델 학습에 쓰지 않는다고 Anthropic 약관과 Privacy Center에
적혀 있다. 개인 요금제(Free·Pro·Max)는 설정에 따라 학습에 쓰인다. 다만 학습 여부와 커밋
여부는 다른 질문이다. 스킬이 묻는 것은 "이 문서를 git 이력에 영구히 남겨도 되는가"이고,
아니면 vault 밖 변환 모드로 간다. 정책 문장이 아니라 경로로 닫는다. 한 가지는 정직하게
덧붙인다 — vault 밖 변환 모드는 git 이력을 피한 것이고 로컬 흔적을 지운 것이 아니다. 변환
세션 전사본은 `~/.claude/projects/`에 평문으로 기본 30일 남는다
**근거**: `week2-sources/04-knowledge-base.md` § 6 (Anthropic Commercial Terms
2025-06-17, Privacy Center, Claude Code Data usage) · `week2-sources/05-work-usage.md`
§ 4 (게이트 3개 순서, vault 밖 변환 모드)

---

## 11 다음 주

> 2분 · 슬라이드 1장

오늘 다룬 개념이 3주차와 4주차에서 어디로 이어지는지 위치를 잡는다.

### 3주차와 4주차

3주차는 클라우드 연동이다. Google 드라이브·슬라이드·시트·메일을 붙이고, 텔레그램·카카오톡
으로 알림을 받는다. 오늘 본 MCP가 그 준비였다.

4주차는 스킬을 직접 만든다. 오늘 본 `SKILL.md` 형식으로 자기 업무 절차를 문서로 고정한다.
매번 설명하던 절차를 한 번 적어 두고 그다음부터는 이름으로 부른다.

과제는 문서 1건이다. 다음 주 시작할 때 확인한다.

**화면**: 4주 로드맵 한 줄 — 1주 vault 구축 → 2주 문서 반입 → 3주 클라우드 연동 → 4주
스킬 제작. 오늘 자리에 표시를 둔다
**말할 것**: 오늘 MCP를 3분만 다룬 이유가 여기 있다. 3주차 전체가 MCP를 쓰는 시간이다
**근거**: `week2-outline.md` § 11

---

## 검증 필요

- **`vault-query` 실효 임계.** 노트가 몇 개부터 질의가 쓸모 있어지는지에 대한 실측 근거가
  없다(`05-work-usage.md` § 5). 슬라이드에서 "넣는 만큼 답이 좋아진다"로만 썼고 수치는
  넣지 않았다. 시연 중 노트 4개로 질의를 실제로 돌려 보면 답이 얼마나 얇은지 그 자리에서
  드러날 수 있다. 발표 전에 한 번 돌려 보고, 답이 빈약하면 그 사실을 그대로 보여주는 쪽이
  낫다.
- ~~`결과 확인` 표의 `Clippings/` 행~~ — **2026-08-28 검수에서 해소. 삭제가 아니라 이동이다.**
  커밋 `a7d1f61`의 diff가 `{Clippings => raw}/점검-결과-보고.md`로 잡힌다. git이 rename으로
  인식했으므로 내용 변경 없는 이동이다. 표와 본문 문구를 "옮기면서 비워진다"로 고쳤다.
  시연에서 이 rename을 `git log --stat`으로 보여줄 수도 있다.
- **주간 보고서 `.html` 뷰.** 파일은 템플릿 계약 검사를 통과했다 (`<style>` 블록 0 ·
  flex/grid 0 · media query 0 · `role="presentation"` 29개 · 20,112 B로 Gmail 클리핑 한도
  102 KB 이내). 다만 브라우저 렌더를 눈으로 확인하지는 않았다. 시연 전에 한 번 열어 본다.
- **개인정보 답변의 보관 기간.** `~/.claude/projects/` 평문 30일과 `cleanupPeriodDays`는
  `04-knowledge-base.md` § 6에 적힌 범위다. ZDR·요금제별 보관 기간은 시트에 더 있으나
  3분 슬라이드에 넣지 않았다. 질문이 더 깊게 들어오면 시트를 열어 답한다.
- **과제 4단계의 예상 소요.** 수강자 1인이 문서 1건으로 4단계를 마치는 데 걸리는 시간을
  재본 근거가 없다. "한 건이면 된다"로만 안내했다.
